const c = require('chalk')
export const {log} = console

export function logHeading(str) {
  log()
  log(c.underline(str))
  log()
}

export function padlog(...args) {
  log()
  log(...args)
  log()
}
